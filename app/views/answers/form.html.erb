<% if @question.id.present? %>
	<%= form_for([@exam, @question, @answer]) do |f| %>
	  <div class="field">
	    <%= f.label :desc %><br />
	    <%= f.text_area :desc %>
	  </div>
	  <%= f.file_field :image %>
	  <div class="actions">
	    <%= f.submit %>
	  </div>
	<% end %>
<% end %>
