<% if @question.id.present? %>
	<%= form_for([@exam, @question, @answer]) do |f| %>
	  <div class="field">
	    <%= f.label :desc %><br />
	    <%= f.text_area :desc %>
	  </div>
	  <div class="actions">
	    <%= f.submit %>
	  </div>
	<% end %>
<% end %>
